# When running inside Docker, the frontend container hits the backend via the Docker service name.
# When running locally (npm run dev), use VITE_API_URL from .env instead.
VITE_API_URL=http://backend:8080
